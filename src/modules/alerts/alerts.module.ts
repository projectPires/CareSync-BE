import { Module } from '@nestjs/common';

/**
 * Alerts module — rules engine, lifecycle (ack/resolve/escalate), Socket.IO gateway.
 * Implementation: issues #20, #21.
 */
@Module({})
export class AlertsModule {}
