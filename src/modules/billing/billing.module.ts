import { Module } from '@nestjs/common';

/**
 * Billing module — subscription read-only, Stripe webhooks, grace-period state machine.
 * Implementation: issue #25.
 */
@Module({})
export class BillingModule {}
