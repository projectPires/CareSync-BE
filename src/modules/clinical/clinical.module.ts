import { Module } from '@nestjs/common';

/**
 * Clinical module — eMAR, vitals, log entries, wounds, elimination, activities.
 * Implementation: issues #6, #8, #10, #11, #12.
 */
@Module({})
export class ClinicalModule {}
